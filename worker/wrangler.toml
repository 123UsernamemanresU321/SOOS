name = "session-order-os-worker"
main = "worker.js"
compatibility_date = "2024-12-01"

# The DEEPSEEK_API_KEY must be set as a secret:
#   wrangler secret put DEEPSEEK_API_KEY
# Never put the actual key in this file.
